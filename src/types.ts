import { LucideProps } from "lucide-react";

export type TestimonialType = {
  id: string;
  name: string;
  role: string;
  content: string;
  avatar: string;
};

export type FAQType = {
  question: string;
  answer: string;
};
